{
  "name": "portafolio-web",
  "version": "1.0.0",
  "description": "Portfolio website - Full stack",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm install && npm run build"
  }
}
